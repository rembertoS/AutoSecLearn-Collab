import SwiftUI

struct RembertView: View {
    var body: some View {
        NavigationStack {
            Text("Rembert Silva")
                .navigationTitle("Rembert's Section")
        }
    }
}
